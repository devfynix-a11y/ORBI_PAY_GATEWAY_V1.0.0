{
  "name": "@orbifinancial/pay-gateway",
  "version": "0.1.2",
  "description": "Official ORBI Pay Gateway SDK for payment intents, PaySafe escrow, payment profiles, identity resolution, and webhook verification.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "orbi-pay-gateway": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test --test-force-exit tests/**/*.test.ts",
    "check": "npm run build && npm test"
  },
  "keywords": [
    "orbi",
    "payments",
    "paysafe",
    "escrow",
    "baas",
    "open-banking"
  ],
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
