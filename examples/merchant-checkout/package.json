{
  "name": "orbi-merchant-checkout-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal ORBI Pay Gateway merchant checkout example using the Node SDK.",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test --test-force-exit tests/**/*.test.ts",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@orbi/pay-gateway": "file:../../sdk/node",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.19.15",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22"
  }
}
